public class Rev {
    public static void revn(Integer num){
        Integer n=num;
        Integer sum=0;
        while(n>0){
            Integer d=Math.mod(n,10);
            sum=sum*10+d;
            n=n/10;
        }
        System.debug(sum);
    }

}
